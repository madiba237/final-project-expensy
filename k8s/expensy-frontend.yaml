apiVersion: apps/v1
kind: Deployment
metadata:
  name: expensy-frontend
  namespace: default
  labels:
    app: expensy-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: expensy-frontend
  template:
    metadata:
      labels:
        app: expensy-frontend
    spec:
      imagePullSecrets:
        - name: drs
      containers:
        - name: frontend
          image: mulema/expensy-nextjs-frontend:latest 
          ports:
            - containerPort: 80
          env:
            # Variable d'environnement lue par le frontend pour joindre l'API backend interne
            - name: BACKEND_URL
              value: "http://expensy-backend-service:5000"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: expensy-frontend-service
  namespace: default
 
spec:
  type: ClusterIP # Génère un Load Balancer AWS avec une URL DNS publique
  selector:
    app: expensy-frontend
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP